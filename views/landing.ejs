<% include ./partials/header %>
  <div class="container">
      <% if(error && error.length > 0){ %>
          <div class="alert alert-danger" role="alert">
              <%= error %>
          </div>
      <% } %>
      <% if(success && success.length > 0){ %>
          <div class="alert alert-success" role="alert">
              <%= success %>
          </div>
      <% } %>
  </div>

  <div id="landing-header">
    <% if(currentUser){ %>
      <a href="/gameBoard" class="btn btn-lg btn-success">Game Board</a>
    <% } %>
      <%# <a href="/joingteam" class="btn btn-lg btn-success">Join a team</a> %>
	  <!-- <a href="/register" class="btn btn-lg btn-success">Create New User</a> -->
    <!-- <a href="/login" class="btn btn-lg btn-success">Continue Game</a> -->
    <!-- <a href="/gameBoard" class="btn btn-lg btn-success">Game Board</a> -->
  </div>

<% include ./partials/footer %>
